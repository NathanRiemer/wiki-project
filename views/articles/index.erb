<h1>Articles</h1>

<ul>
  <% @articles.each do |article| %>
    <li><%= build_link(["articles", article.id], article.title) %></li>
  <% end %>
</ul>

<% if @user %>
  <p><%= build_link(["articles", "new"], "Create New Article") %></p>
<% end %>